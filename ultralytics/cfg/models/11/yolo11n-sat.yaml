# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Ultralytics YOLO11 object detection model with P3/8 - P5/32 outputs
# Model docs: https://docs.ultralytics.com/models/yolo11
# Task docs: https://docs.ultralytics.com/tasks/detect

# Parameters
nc: 7 # number of classes
scales: # model compound scaling constants, i.e. 'model=yolo11n.yaml' will call yolo11.yaml with scale 'n'
  n: [0.50, 0.25, 1024] # summary: 181 layers, 2624080 parameters, 2624064 gradients, 6.6 GFLOPs

# YOLO11n backbone
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2（不变）
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4（不变）
  # 2层：C3k2CE→TAFM，参数：dim=256, window=7, heads=4, reduction=4（对应ratio=0.25）
  - [-1, 2, SAT_TAFM, [256, 7, 4, 4, False]] 
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8（不变）
  # 4层：C3k2CE→TAFM，dim=512, reduction=4
  - [-1, 2, SAT_TAFM, [512, 7, 4, 4, False]] 
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16（不变）
  # 6层：C3k2CE→TAFM，dim=512, shortcut=True
  - [-1, 2, SAT_TAFM, [512, 7, 4, 16, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# YOLO11n head
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 11（不变）
  - [[-1, 6], 1, Concat, [1]] # 12（不变）
  # 13层：C3k2CE→TAFM，dim=512, shortcut=False
  - [-1, 2, SAT_TAFM, [512, 7, 4, 16, False]] 

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 14（不变）
  - [[-1, 4], 1, Concat, [1]] # 15（不变）
  # 16层：C3k2CE→TAFM，dim=256, shortcut=False
  - [-1, 2, SAT_TAFM, [256, 7, 4, 16, False]] 

  - [-1, 1, Conv, [256, 3, 2]] # 17（不变）
  - [[-1, 13], 1, Concat, [1]] # 18（不变）
  # 19层：C3k2CE→TAFM，dim=512, shortcut=False
  - [-1, 2, SAT_TAFM, [512, 7, 4, 16, False]]

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 10], 1, Concat, [1]] # cat head P5
  - [-1, 2, C3k2, [1024, True]] # 22 (P5/32-large)

  - [[16, 19, 22], 1, Detect, [nc]] # Detect(P3, P4, P5)